import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

// Initialize the application and render to DOM
createRoot(document.getElementById("root")!).render(<App />);
